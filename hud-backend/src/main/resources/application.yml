server:
  port: 8888
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/hud}
    username: ${DB_USER:huduser}
    password: ${DB_PASS:hudpass}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

langchain4j:
  ollama:
    chat-model:
      base-url: http://inference.jakefear.com:11434
      model-name: gemma4:e4b
      num-ctx: 131072
      timeout: PT10M
      temperature: 0.0
